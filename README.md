#python instaloader module
import instaloader

#create an instance
test = install.instaloader()

def get_pro (a) :
  test.download_profile(a,profile_pic_only=True)
  
 a=input("username>>>>")
 get_pro(a)
